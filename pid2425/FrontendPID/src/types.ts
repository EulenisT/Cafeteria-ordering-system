export type SandwichesResponse = {
    code: string;
    nom: string;
    disponible: boolean;
    prix: number;
}

export type GarnitureResponse = {
    code: string;
    nom: string;
    disponible: boolean;
}

